name: flare_rendering_issue
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flare_flutter:
  flare_dart:

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/searching.flr
    - assets/searchingOther.flr
    - assets/Loading.flr
    - assets/dragon.flr
    - assets/bridge.flr
